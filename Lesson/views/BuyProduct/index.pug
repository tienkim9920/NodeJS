doctype html
head
  meta(charset='UTF-8')
  meta(name='viewport', content='width=device-width, initial-scale=1.0')
  link(rel='stylesheet', href='https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css',
   integrity='sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh', crossorigin='anonymous')
  title Product

.container
    h1.text-center(style="padding: 3rem 0") Danh Sách Sản Phẩm
    .row
        each user in users
            .col-lg-3
                .card(style='width: 17rem; margin: 1rem 1rem')
                    img.card-img-top.text-center(src='/' + user.avatar, style="height: 270px")
                    .card-body.text-center
                        h5.card-title= user.name
                        p.card-text=user.price
                        a.btn.btn-success(href='/BuyProduct/' + user._id) Đặt Hàng
